fn main() {
	/*
		ooo fancy block comment
	*/
	let a = 1, b = 2;
	let mut c = 3, d = 4;

	a > b >= c < d <= e == f != g;
	a + b - c * d / e % f;
	a ^ b & c | d >> e << f;
	a * (b + c); // parentheses
	a = b === c >>= d; // right-associativity
	!~<f64!><f64>--+++-&*a;
}

// comment that ends with eof