fn main(): void {
	let a = 1;
	let b = 2;
	let mut c = 3;
	let mut d = 4;
	a > b >= c < d >= e == f != g;
	a + b - c * d / e % f;
	a ^ b & c | d >> e << f;
	a * (b + c);
	a = b === c >>= d;
	!~<f64!><f64>--+++-&*a++--;
}